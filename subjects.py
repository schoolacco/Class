class Subject:
    def __init__(self, name, year, code, num):
        global List
        global Class_List
        self.name = name
        self.year = year
        self.code = code
        self.num = num
